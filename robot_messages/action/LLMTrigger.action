# Request
string prompt     # Prompt from user

---
# Response
bool success         # True if the action succeeded, false otherwise
string message       # A message indicating the success or error status
string llm_response  # The JSON object returned from the LLM (as a string)

---
# Feedback
string status       # Status updates on the action's progress